<div class="d-flex flex-wrap">
  <% items.each do |item| %>
  <div class="me-2">
    <div>
      <%= link_to item_path(item.id) do %>
        <%= image_tag item.image, size: '200x100' %>
      <% end %>
    </div>
    <p><%= item.name %></p>
    <p>¥<%= item.price %></p>
  </div>
  <% end %>
</div>